services:
  djambi.shorttempstore:
    class: Drupal\djambi\Services\ShortTempStoreFactory
    tags:
      - { name: needs_destruction }
    arguments: ['@serialization.phpserialize', '@database', '@lock']