<div class="piece piece-type-{{ piece.type }} piece-status-{{ piece.alive ? 'alive' : 'dead' }} piece-movable-{{ piece.isMovable() ? 'yes' : 'no' }} faction-{{ piece.faction.class }} faction-origine-{{ piece.faction.control.class }} {{ extra_classes }}" data-piece-id="{{ piece.id }}">{{ piece_representation }}</div>